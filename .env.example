# ------------------------------------------------------------
# PicoClaw Docker Compose Environment
# Copie este arquivo para .env e ajuste os valores.
# ------------------------------------------------------------

# Repositorio e branch usados pelo servico gateway para clonar/atualizar codigo
REPO_URL=https://github.com/caam1406/picoclaw.git
REPO_BRANCH=main # main ou v1.0.0 (versao oficial do PicoClaw)

# Fuso horario do container
TZ=America/Sao_Paulo

# Portas expostas no host
PICOCLAW_GATEWAY_PORT=18790
PICOCLAW_DASHBOARD_PORT=18791

# Dashboard auth
# Opcional: se vazio, sera gerado automaticamente e persistido em volume de secrets.
DASHBOARD_TOKEN=

# Provider inicial (OpenRouter) para primeira subida
# IMPORTANTE: preencha com sua chave valida
OPENROUTER_API_KEY=

# ------------------------------------------------------------
# PostgreSQL (banco de dados integrado)
# ------------------------------------------------------------
# Credenciais do banco. Se POSTGRES_PASSWORD estiver vazio,
# uma senha segura sera gerada automaticamente pelo init-secrets
# e persistida no volume de secrets.
POSTGRES_USER=picoclaw
POSTGRES_PASSWORD=
POSTGRES_DB=picoclaw

# Storage (postgres | file)
# Com o servico PostgreSQL integrado, o padrao e "postgres".
PICOCLAW_STORAGE_TYPE=postgres

# Override manual da URL de conexao (opcional).
# Se vazio, a URL sera construida automaticamente a partir das
# variaveis POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB e POSTGRES_HOST.
# Formato: postgres://user:password@host:5432/dbname?sslmode=disable
PICOCLAW_STORAGE_DATABASE_URL=

# Caminho para storage local (usado apenas se PICOCLAW_STORAGE_TYPE=file)
# PICOCLAW_STORAGE_FILE_PATH=~/.picoclaw/workspace/sessions

# ------------------------------------------------------------
# GogCLI (opcional; deixe false para menor consumo)
# ------------------------------------------------------------
PICOCLAW_TOOLS_GOGCLI_ENABLED=false
PICOCLAW_TOOLS_GOGCLI_DEFAULT_ACCOUNT=
# PICOCLAW_TOOLS_GOGCLI_BINARY=          # deixe vazio para auto-detectar "gog" ou "gogcli"
# PICOCLAW_TOOLS_GOGCLI_TIMEOUT_SECONDS=90

# Google OAuth (necessario para GogCLI)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
OAUTHLIB_INSECURE_TRANSPORT=1
GOG_ACCOUNT=
