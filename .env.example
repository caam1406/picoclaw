# ------------------------------------------------------------
# PicoClaw Docker Compose Environment
# Copie este arquivo para .env e ajuste os valores.
# ------------------------------------------------------------

# Repositorio e branch usados pelo serviço gateway para clonar/atualizar código
REPO_URL=https://github.com/caam1406/picoclaw.git
REPO_BRANCH=main

# Fuso horario do container
TZ=America/Sao_Paulo

# Portas expostas no host
PICOCLAW_GATEWAY_PORT=18790
PICOCLAW_DASHBOARD_PORT=18791

# Dashboard auth
# Opcional: se vazio, sera gerado automaticamente e persistido em volume de secrets.
DASHBOARD_TOKEN=

# Provider inicial (OpenRouter) para primeira subida
# IMPORTANTE: preencha com sua chave valida
OPENROUTER_API_KEY=

# PostgreSQL
POSTGRES_DB=picoclaw
POSTGRES_USER=picoclaw
# Opcional: se vazio, sera gerado automaticamente e persistido em volume de secrets.
POSTGRES_PASSWORD=
# Hostname do PostgreSQL visivel para o container do gateway.
# Em alguns ambientes (ex: Dokploy/stack), pode nao ser "postgres".
POSTGRES_HOST=postgres

# Storage (aplicado automaticamente no config DB ao iniciar via compose)
PICOCLAW_STORAGE_TYPE=postgres
# Se vazio, o compose monta automaticamente com POSTGRES_*.
PICOCLAW_STORAGE_DATABASE_URL=
PICOCLAW_STORAGE_FILE_PATH=~/.picoclaw/workspace/sessions
PICOCLAW_STORAGE_SSL_ENABLED=false

# Config store (segredos + configuracoes) no mesmo PostgreSQL
# Se vazio, o compose monta automaticamente com POSTGRES_*.
PICOCLAW_CONFIG_DATABASE_URL=
